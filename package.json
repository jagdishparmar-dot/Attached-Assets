{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@9.2.0",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev": "concurrently -n api,admin -c blue,green \"pnpm dev:api\" \"pnpm dev:admin\"",
    "dev:api": "dotenv -- pnpm --filter @workspace/api-server run dev",
    "dev:admin": "dotenv -- pnpm --filter @workspace/admin run dev",
    "db:push": "dotenv -- pnpm --filter @workspace/db run push",
    "db:seed": "dotenv -- pnpm --filter @workspace/scripts run seed-staff",
    "db:setup": "pnpm db:push && pnpm db:seed"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^16.4.7",
    "dotenv-cli": "^8.0.0",
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
